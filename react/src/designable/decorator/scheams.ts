import { ColSchema, RowSchema, RouterLinkSchema, ContainerSchema, DropdownSchema, TagSchema } from '../schemas'

export const Schemas = {
  Col: ColSchema,
  Row: RowSchema,
  RouterLink: RouterLinkSchema,
  Container: ContainerSchema,
  Dropdown: DropdownSchema,
  TagSchema,
}